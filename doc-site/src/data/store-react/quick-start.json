{
    "id": "quick-start",
    "title": "Quick Start",
    "subtitle": "React State Management in 2 Minutes",
    "description": "Learn how to use qortex-store-react to manage state in React components.",
    "category": "Getting Started",
    "status": "stable",
    "content": "qortex-store-react re-exports everything from qortex-store, so you only need one import.",
    "sections": [
        {
            "title": "1. Create a Store",
            "content": "Define your store — import `createStore` directly from `qortex-store-react`:",
            "code": {
                "language": "typescript",
                "code": "import { createStore } from 'qortex-store-react';\n\nconst counterStore = createStore((set, get) => ({\n  count: 0,\n  increment: () => set({ count: get().count + 1 }),\n  decrement: () => set((s) => ({ count: s.count - 1 })),\n  reset: () => set({ count: 0 }),\n}));"
            }
        },
        {
            "title": "2. Create a Bound Hook (Recommended)",
            "content": "For better ergonomics, create a specialized hook for your store using `createUseStore`. This avoids passing the store as an argument to every hook call.",
            "code": {
                "language": "typescript",
                "code": "import { createUseStore } from 'qortex-store-react';\n\n// Create a specialized hook\nexport const useCounter = createUseStore(counterStore);"
            }
        },
        {
            "title": "3. Use in a Component",
            "content": "Use your bound hook with a selector to subscribe to specific state slices:",
            "code": {
                "language": "tsx",
                "code": "function Counter() {\n  const count = useCounter((s) => s.count);\n  const increment = useCounter((s) => s.increment);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={increment}>+1</button>\n    </div>\n  );\n}"
            }
        },
        {
            "title": "Alternative: Using base useStore",
            "content": "You can also use the base `useStore` hook by passing the store instance directly:",
            "code": {
                "language": "tsx",
                "code": "import { useStore } from 'qortex-store-react';\n\nfunction Counter() {\n  const count = useStore(counterStore, (s) => s.count);\n  return <p>Count: {count}</p>;\n}"
            }
        }
    ]
}