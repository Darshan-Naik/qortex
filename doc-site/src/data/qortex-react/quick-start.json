{
    "id": "quick-start",
    "title": "Quick Start",
    "subtitle": "React Hooks Usage",
    "description": "Learn how to use qortex-react hooks in your components.",
    "category": "Getting Started",
    "status": "stable",
    "content": "Use the useQuery hook to fetch and subscribe to data in your components.",
    "sections": [
        {
            "title": "1. Register a Fetcher",
            "content": "Register a global fetcher (usually in your app entry point):",
            "code": {
                "language": "typescript",
                "code": "import { registerFetcher } from 'qortex-react';\n\nregisterFetcher('users', async () => {\n  const res = await fetch('/api/users');\n  return res.json();\n});"
            }
        },
        {
            "title": "2. Use the Hook",
            "content": "Use useQuery in your components:",
            "code": {
                "language": "tsx",
                "code": "import { useQuery } from 'qortex-react';\n\nfunction UserList() {\n  const { data, isLoading, error } = useQuery('users');\n\n  if (isLoading) return <div>Loading...</div>;\n  if (error) return <div>Error: {error.message}</div>;\n\n  return (\n    <ul>\n      {data.map(user => (\n        <li key={user.id}>{user.name}</li>\n      ))}\n    </ul>\n  );\n}"
            }
        }
    ]
}