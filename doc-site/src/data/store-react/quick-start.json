{
    "id": "quick-start",
    "title": "Quick Start",
    "subtitle": "React State Management in 2 Minutes",
    "description": "Learn how to use @qortex/store-react to manage state in React components.",
    "category": "Getting Started",
    "status": "stable",
    "content": "@qortex/store-react re-exports everything from @qortex/store, so you only need one import.",
    "sections": [
        {
            "title": "1. Create a Store",
            "content": "Define your store — import `createStore` directly from `@qortex/store-react`:",
            "code": {
                "language": "typescript",
                "code": "import { createStore } from '@qortex/store-react';\n\nconst counterStore = createStore((set, get) => ({\n  count: 0,\n  increment: () => set({ count: get().count + 1 }),\n  decrement: () => set((s) => ({ count: s.count - 1 })),\n  reset: () => set({ count: 0 }),\n}));"
            }
        },
        {
            "title": "2. Use in a Component",
            "content": "Use `useStore` with a selector to subscribe to specific state slices:",
            "code": {
                "language": "tsx",
                "code": "import { useStore } from '@qortex/store-react';\n\nfunction Counter() {\n  const count = useStore(counterStore, (s) => s.count);\n  const increment = useStore(counterStore, (s) => s.increment);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={increment}>+1</button>\n    </div>\n  );\n}"
            }
        },
        {
            "title": "3. Without a Selector",
            "content": "Omit the selector to get the full state (re-renders on every change):",
            "code": {
                "language": "tsx",
                "code": "function CounterFull() {\n  const state = useStore(counterStore);\n  return <p>Count: {state.count}</p>;\n}"
            }
        }
    ]
}