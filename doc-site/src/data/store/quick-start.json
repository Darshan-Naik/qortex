{
    "id": "quick-start",
    "title": "Quick Start",
    "subtitle": "State Management in 2 Minutes",
    "description": "Learn how to create and use a store with qortex-store.",
    "category": "Getting Started",
    "status": "stable",
    "content": "qortex-store provides a simple, type-safe API for managing application state. It works anywhere — Node, browser, vanilla JS.",
    "sections": [
        {
            "title": "1. Create a Store",
            "content": "Define your state and actions using `createStore`:",
            "code": {
                "language": "typescript",
                "code": "import { createStore } from 'qortex-store';\n\nconst counterStore = createStore((set, get) => ({\n  count: 0,\n  increment: () => set({ count: get().count + 1 }),\n  decrement: () => set((state) => ({ count: state.count - 1 })),\n  reset: () => set({ count: 0 }),\n}));"
            }
        },
        {
            "title": "2. Read and Update State",
            "content": "Use `getState` and `setState` from anywhere:",
            "code": {
                "language": "typescript",
                "code": "// Read state\ncounterStore.getState().count; // 0\n\n// Update via actions\ncounterStore.getState().increment();\ncounterStore.getState().count; // 1\n\n// Or use setState directly\ncounterStore.setState({ count: 42 });"
            }
        },
        {
            "title": "3. Subscribe to Changes",
            "content": "Listen for state changes and clean up when done:",
            "code": {
                "language": "typescript",
                "code": "const unsub = counterStore.subscribe((state, prev) => {\n  console.log('count:', prev.count, '→', state.count);\n});\n\n// Later, stop listening\nunsub();"
            }
        }
    ]
}