/**
 * Store binders for qortex-form-react.
 *
 * Works with `qortex-store` or any duck-typed `{ get, subscribe }` store.
 * Optional peer: `qortex-store`.
 *
 * ```ts
 * import { useFormStore } from "qortex-form-react/store";
 * ```
 */

export { useFormStore } from "./useFormStore";
export type { FormStoreLike, UseFormStoreConfig } from "./useFormStore";
